import { test } from "@playwright/test";

test.describe("Admin Invites (skeleton)", () => {
  test.fixme("lists invites and navigates to detail", async ({ page }) => {
    // TODO: implement
  });

  test.fixme("creates an invite via form", async ({ page }) => {
    // TODO: implement
  });
});

