import { test } from "@playwright/test";

test.describe("Vendor Print Jobs (skeleton)", () => {
  test.fixme("searches and filters print jobs; navigates to detail", async ({ page }) => {
    // TODO: implement
  });

  test.fixme("updates print job status/date and returns to list", async ({ page }) => {
    // TODO: implement
  });
});

