import { test } from "@playwright/test";

test.describe("Vendor Settings (skeleton)", () => {
  test.fixme("manages payout accounts via dialog and toggles flags", async ({ page }) => {
    // TODO: implement
  });
});

