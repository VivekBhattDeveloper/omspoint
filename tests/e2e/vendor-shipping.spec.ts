import { test } from "@playwright/test";

test.describe("Vendor Shipping (skeleton)", () => {
  test.fixme("navigates to shipment detail and updates tracking/method/date", async ({ page }) => {
    // TODO: implement
  });
});

