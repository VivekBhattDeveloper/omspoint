import { test } from "@playwright/test";

test.describe("Vendor Orders (skeleton)", () => {
  test.fixme("searches and filters orders; navigates to detail", async ({ page }) => {
    // TODO: implement
  });

  test.fixme("edits order fields and returns to list", async ({ page }) => {
    // TODO: implement
  });
});

