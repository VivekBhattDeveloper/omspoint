import "@testing-library/jest-dom/vitest";
import { describe, it } from "vitest";

describe("Vendor Settings", () => {
  it.todo("adds payout account via dialog and validates required fields");
  it.todo("toggles flags (autoPayout, sameDayEnabled) and sets primary");
  it.todo("updates notification routing and escalation contact");
});

