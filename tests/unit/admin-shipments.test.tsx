import "@testing-library/jest-dom/vitest";
import { describe, it } from "vitest";

describe("Admin Shipments", () => {
  it.todo("computes stats and primary method from loader data");
  it.todo("renders table with tracking, method, date, order");
  it.todo("navigates to schedule shipment and detail routes");
});

