import "@testing-library/jest-dom/vitest";
import { describe, it } from "vitest";

describe("Vendor Shipping", () => {
  it.todo("edits shipment tracking, method, and date");
  it.todo("maps unique tracking conflicts to inline error");
  it.todo("navigates back to vendor shipping list on success");
});

