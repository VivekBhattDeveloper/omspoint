import "@testing-library/jest-dom/vitest";
import { describe, it } from "vitest";

describe("Vendor Orders", () => {
  it.todo("applies search and status filter to table");
  it.todo("navigates to order detail on row click");
  it.todo("updates order fields and shows related links when present");
});

