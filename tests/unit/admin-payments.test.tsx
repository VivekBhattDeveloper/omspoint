import "@testing-library/jest-dom/vitest";
import { describe, it } from "vitest";

describe("Admin Payments", () => {
  it.todo("aggregates total volume and method counts from loader data");
  it.todo("renders table with amount, method, captured, order");
  it.todo("navigates to log payment and detail routes");
});

